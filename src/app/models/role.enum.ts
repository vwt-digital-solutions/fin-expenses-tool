export enum Role {
    Employee = 'finance.expenses',
    Manager = 'manager.write',
    LeaseCoordinator = 'leasecoordinator.write',
    Creditor = 'creditor.write',
    Controller = 'controller.write'
}
